requirements.txtversion: "1.0"

build:
  type: python
  runtime: python3.9
  requirements: requirements.txt

run:
  type: python
  command: python3 -m Yukki
version: "1.0"

environment:
  language: python
  version: "3.9"

build:
  requirements: requirements.txt

run:
  command: python3 -m Yukki
